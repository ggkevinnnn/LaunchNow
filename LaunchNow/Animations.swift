import SwiftUI

enum LNAnimations {
    static let smooth: Animation = .smooth
    static let easeInOut: Animation = .easeInOut(duration: 0.3)
    static let folder: Animation = .smooth(duration: 0.35)
}

